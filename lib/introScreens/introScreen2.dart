import 'package:flutter/material.dart';

class introPage2 extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow[100],
      child: Center(
        child: Text('Page 2'),
        //Introducir aqui las imagenes y los contenidos
      ),
    );
  }
}